Genera el dashboard y despliégalo a Apps Script:

1. Corre `python gen_dashboard_html.py` desde la carpeta `Code_ADQ_Dash/`
2. Corre `python deploy_appscript.py` desde la carpeta `Code_ADQ_Dash/`
3. Reporta la URL final del dashboard
